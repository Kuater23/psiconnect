import 'package:flutter/material.dart';

class ProfessionalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Professional Page'),
      ),
      body: Center(
        child: Text('Welcome to the Professional Page!'),
      ),
    );
  }
}